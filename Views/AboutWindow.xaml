<Window x:Class="OtomatikMetinGenisletici.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="ℹ️ Hakkında" 
        Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource SystemControlPageBackgroundChromeLowBrush}"
        ui:WindowHelper.UseModernWindowStyle="True">
    
    <Grid>
        <!-- Background Gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#2196F3" Offset="0"/>
                <GradientStop Color="#21CBF3" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="20">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <!-- App Title -->
                <TextBlock Text="Otomatik Metin Genişletici"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,8"/>

                <!-- Version -->
                <TextBlock Text="Sürüm 2.0.0 (.NET 8)"
                           FontSize="18"
                           Foreground="White"
                           Opacity="0.9"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <!-- Info Card -->
                <Border Background="White"
                        CornerRadius="12"
                        Padding="40"
                        MinWidth="600"
                        MaxWidth="700">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.2" ShadowDepth="4" BlurRadius="12"/>
                    </Border.Effect>

                    <StackPanel>
                        <!-- Developer Info -->
                        <TextBlock Text="👨‍💻 Geliştirici"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,20"/>

                        <!-- Name -->
                        <TextBlock Text="Saffet Çelik"
                                   FontSize="28"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   Margin="0,0,0,30"/>

                        <!-- Contact Info Section -->
                        <StackPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                            <!-- Email -->
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,15">
                                <TextBlock Text="📧" FontSize="20" Margin="0,0,12,0"/>
                                <TextBlock Text="iletisim@saffetcelik.com.tr"
                                           FontSize="16"
                                           Foreground="#2196F3"
                                           Cursor="Hand"
                                           MouseLeftButtonUp="Email_Click"
                                           FontWeight="Medium"/>
                            </StackPanel>

                            <!-- GitHub -->
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,15">
                                <TextBlock Text="🐙" FontSize="20" Margin="0,0,12,0"/>
                                <TextBlock Text="github.com/saffetcelik"
                                           FontSize="16"
                                           Foreground="#2196F3"
                                           Cursor="Hand"
                                           MouseLeftButtonUp="GitHub_Click"
                                           FontWeight="Medium"/>
                            </StackPanel>

                            <!-- Instagram -->
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,15">
                                <TextBlock Text="📷" FontSize="20" Margin="0,0,12,0"/>
                                <TextBlock Text="instagram.com/kamucoder"
                                           FontSize="16"
                                           Foreground="#E4405F"
                                           Cursor="Hand"
                                           MouseLeftButtonUp="Instagram_Click"
                                           FontWeight="Medium"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Description -->
                        <TextBlock Text="Modern .NET 8 teknolojileri ile geliştirilmiştir"
                                   FontSize="14"
                                   Foreground="Gray"
                                   HorizontalAlignment="Center"
                                   Margin="0,10,0,0"
                                   FontStyle="Italic"/>

                        <!-- Features -->
                        <TextBlock Text="🚀 Özellikler"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,20,0,16"/>

                        <StackPanel Margin="0,0,0,20" HorizontalAlignment="Center">
                            <TextBlock Text="✅ Hızlı metin genişletme ve kısayollar" Margin="0,3" FontSize="14" HorizontalAlignment="Center"/>
                            <TextBlock Text="✅ Akıllı metin önerileri" Margin="0,3" FontSize="14" HorizontalAlignment="Center"/>
                            <TextBlock Text="✅ Özelleştirilebilir tuş kombinasyonları" Margin="0,3" FontSize="14" HorizontalAlignment="Center"/>
                            <TextBlock Text="✅ Pencere filtreleme sistemi" Margin="0,3" FontSize="14" HorizontalAlignment="Center"/>
                            <TextBlock Text="✅ Öğrenme algoritması" Margin="0,3" FontSize="14" HorizontalAlignment="Center"/>
                            <TextBlock Text="✅ Modern ve kullanıcı dostu arayüz" Margin="0,3" FontSize="14" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Copyright -->
                        <TextBlock Text="© 2024 Saffet Çelik - Tüm hakları saklıdır"
                                   FontSize="12"
                                   Foreground="Gray"
                                   HorizontalAlignment="Center"
                                   Margin="0,20,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Close Button -->
                <Button Content="✅ Tamam" 
                        Style="{StaticResource SuccessButtonStyle}"
                        Click="CloseButton_Click"
                        Margin="0,32,0,0"
                        Padding="24,12"
                        FontSize="16"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
